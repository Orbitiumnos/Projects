import pandas as pd
import numpy as np
import csv

# utf-8 cp1251
df = pd.read_csv("C:\Jypiter\Glob_terror.csv", encoding='latin1', error_bad_lines = False, warn_bad_lines = False, low_memory=False)
df.head(10)

features = ['iyear', 'imonth', 'iday', 'country_txt', 'region_txt', 'latitude', 'longitude']

df = df.loc[:,features]
df.head()

import cx_Oracle
conn = cx_Oracle.connect('SYSTEM/legomania97@//localhost:1521/xe') # логин/пароль@сервер:порт/база данных
print(conn.version)
cursor = conn.cursor()
statement = 'SELECT * FROM specialities'
cursor.execute(statement)
data = cursor.fetchall()
print(data)
conn.close()
